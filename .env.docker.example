# ==============================================
# Campus Marketplace Docker Environment Variables
# ==============================================
# Copy this file to .env for Docker Compose
#   cp .env.docker.example .env
#
# IMPORTANT:
# - This file should be committed to git as a template
# - The actual .env file should NEVER be committed

# ==============================================
# DATABASE CONFIGURATION
# ==============================================

# PostgreSQL Database Configuration
DB_ROOT_PASSWORD=change_root_password!
DB_HOST=localhost
DB_PORT=5432
DB_NAME=campus_marketplace

# Application Database User (Read/Write/Admin permissions)
DB_APP_USER=cm_app_user
DB_APP_PASSWORD=changeme_secure_password

# Read-only Database User (Analytics/Reporting)
DB_READONLY_USER=change_readonly_user
DB_READONLY_PASSWORD=change_readonly_password

# pgAdmin Configuration
PGADMIN_EMAIL=change_pgadmin_email
PGADMIN_PASSWORD=change_pgadmin_password

# Redis Configuration (Optional)
REDIS_PASSWORD=change_redis_password

# ==============================================
# APPLICATION CONFIGURATION
# ==============================================

# Application Port (external access)
APP_PORT=8080

# ==============================================
# JWT CONFIGURATION
# ==============================================

# JWT Secret Key (MUST be at least 256 bits for HS256)
# Generate a secure key: openssl rand -base64 64
JWT_SECRET=myVerySecureSecretKeyForJWTTokensThatShouldBeAtLeast256BitsLongForHS256AlgorithmUpdated2024

# JWT Token Expiration (in milliseconds)
JWT_ACCESS_TOKEN_EXPIRATION=3600000    # 1 hour
JWT_REFRESH_TOKEN_EXPIRATION=604800000 # 7 days

# ==============================================
# SMTP CONFIGURATION
# ==============================================
SMTP_HOST=smtp.change_host.com
SMTP_PORT=587
SMTP_USERNAME=apikey
SMTP_PASSWORD=put_apikey_here

# From email address (sender)
EMAIL_FROM=no-reply@change_email.com

# ==============================================
# SSL CONFIGURATION
# ==============================================
# (disable for development, require for production)
SSL_MODE=disable
SSL_CERT_PATH=/etc/ssl/certs/server.crt
SSL_KEY_PATH=/etc/ssl/private/server.key

# ==============================================
# BACKUP CONFIGURATION
# ==============================================
BACKUP_RETENTION_DAYS=7
REMOTE_BACKUP_RETENTION_DAYS=30
BACKUP_S3_BUCKET=change_bucket_name
AWS_ACCESS_KEY_ID=change_aws_access_key
AWS_SECRET_ACCESS_KEY=change_aws_secret_key
AWS_REGION=change_aws_region

# ==============================================
# LOGGING CONFIGURATION
# ==============================================

# Log Levels (TRACE, DEBUG, INFO, WARN, ERROR)
LOG_LEVEL=INFO
SQL_LOG_LEVEL=WARN

# ==============================================
# SECURITY NOTES
# ==============================================

# 1. NEVER commit the actual .env file to version control
# 2. Use strong, unique passwords for all credentials
# 3. Generate a cryptographically secure JWT secret:
#    openssl rand -base64 64
# 4. Use different values for development and production
# 5. Rotate credentials regularly in production

# ==============================================
# QUICK START
# ==============================================

# 1. Copy this file:
#    cp .env.docker.example .env

# 2. Update passwords and secrets above

# 3. Start services:
#    docker-compose up -d

# 4. Check logs:
#    docker-compose logs -f backend

# 5. Access application:
#    http://localhost:8080/api
